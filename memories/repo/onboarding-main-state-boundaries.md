- Onboarding main selection now has two hard guards in welcome-bot.js: submitSessions normalize mainCharacterIds through getValidatedLiveMainCharacterIds, and pending/manual-approved record creation rejects empty stale main sets instead of silently writing partial or empty mains.
- Shared profile may still preserve archived main ids as label fallback via deriveProfileMainView; that is intentional for profile display, but onboarding submit/manual-approve paths must not consume that raw fallback directly.
- Dormant mainDraft routing was removed from welcome-bot.js and src/onboard/begin-state.js; resumable onboarding now goes only through submitSession or fresh picker open, not through a separate draft seam.
- For smoke coverage that drives completeMainSelection through live-role validation, seed db.config.generatedRoles.characters plus a matching guild role cache entry; most bot.config character roleId slots are blank, so relying on runtime role creation in harnesses is brittle.